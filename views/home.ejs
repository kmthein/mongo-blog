<%- include("./includes/head") %>
<div class="flex navbar space-between p-50">
  <div class="flex">
    <i class="fa-solid fa-blog text-white f-20"></i>
    <h2 class="title text-white"><a href="/" class="bg-none">My Blogs</a></h2>
  </div>
    <% if(isLogin) { %>
  <div class="flex gap-15">
    <p class="text-white">Hello, <%= currentUser %></p>
    <div>
    <a href="/admin/create-post">Add a new post</a>
  </div>
    <div>
    <form action="/logout" method="post">
      <input hidden value="<%= csrfToken %>" name="_csrf" />
      <button>Logout</button>
    </form>
    </div>
  </div>
    <% } else { %>
  <div class="flex gap-15">
    <a href="/login">Login existing account</a>
    <a href="/register">Create new account</a>
    <% } %>
  </div>
</div>
    <hr />
    <% if(loginSuccessMsg) { %>
      <div class="success-box-container">
      <p class="success-box"><%= loginSuccessMsg %></p>
      </div>
    <% } %>
    <% if(postsArr.length === 0 && isLogin) { %>
    <div>
      <h4>Nothing to see ?</h4>
      <p>Create a new post now !!!</p>
      <a href="/admin/create-post">Create a new post</a>
    </div>
    <% } %> 
    <% if(postsArr.length === 0 && !isLogin) { %>
    <div>
      <h4>Login to create new post</h4>
      <p>There is no recent post here.</p>
    </div>
    <% } %>
    <div class="post-container">
    <% for(let post of postsArr) { %>
    <div class="post-box">
      <div>
      <img src="<%= post.imgUrl %>" alt="<%= post.title %>" class="home-img">
        <p class="post-title"><%= post.title %></p>
        <p>Posted by @<%= post.userId.username %></p>
      </div>
      <a class="btn" href="/post/<%= post._id %>">See More...</a>
    </div>
    <% } %>
  </div>
    <%- include("./includes/foot") %>
